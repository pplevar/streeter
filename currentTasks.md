
```
In `walk detail` screen create a button `Recalculate route` which should startrecalculation the route
```

```
The route calculation is quite long so it would be good if we can see the process.
So we need to show the percentahe of calculation process in `walk detail` screen when we doing 
processing
```

